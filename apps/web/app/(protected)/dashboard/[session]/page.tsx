import Navbar from "@/components/navbar";
import React from "react";

const page = () => {
  // fetch user profile and projects

  // if no profile found, send auth required page to vie project msg

  // create a project provider component which takes the projects and initializes all the projects in the state
  // Project list component

  return (
    <>
      <Navbar />
    </>
  );
};

export default page;
