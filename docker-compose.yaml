version: "3.9"

services:
  db:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: s2c
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/
    
volumes:
  db_data:
